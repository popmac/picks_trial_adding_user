<%
  domain = if Rails.env.production?
      'heroku.com'
    else
      'http://localhost:3000'
    end
  url = domain + "/customer/input_password?id=#{@token.id}&token=#{@token.value}"
%>

パスワード再登録の申請がありました。
以下、URLより24時間以内にパスワードの再登録をお願い申し上げます。

<%= url %>

本メールに対する返信メールは、お受けしておりません。
万一お心当たりのない場合は、このメールを破棄してください。
